export interface Post {
    body: string,
    image?: number,
    image_source?: string
    date?: string
    id?: number
    author_first_name?: string
    author_last_name?: string
    liked?: boolean
}